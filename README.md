khởi động MinIO trên terminal:
cd C:\minio
.\minio.exe server D:\minio-data --console-address ":9001"

khởi động Neo4j : Start Database: app 
chạy trên terminal:
python -m uvicorn app.main:app --reload
